---
"@igrp/template-migrator": patch
---

- Add migration `20-sidebar-trigger-in-header`: flips the demo-legacy template's `showIGRPSidebarTrigger` to `true` and resyncs all `@igrp/*` deps to the beta.158 framework set (next-ui beta.157, next beta.158), matching what the create-template zip resolves for fresh scaffolds.
