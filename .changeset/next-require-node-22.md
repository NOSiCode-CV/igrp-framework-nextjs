---
"@igrp/framework-next": patch
---

Require Node `>=22` (`engines.node`), aligning with the rest of the framework (the repo standard is Node 22, and `@igrp/framework-next-types` already requires it). This package previously still advertised `>=20`.
