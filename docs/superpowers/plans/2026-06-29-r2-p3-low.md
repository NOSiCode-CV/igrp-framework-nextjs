# R2 P3 — Low Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clear the 22 P3-Low findings (spec items 31–52) — typos, dead code, copy/i18n, a11y nits, doc notes, and small robustness fixes — across `design-system`, `next`, `next-auth`, `next-ui`, `templates/demo-v1`, and `template-migrator`. These are polish, not behavior rewrites; group trivial sibling fixes per package, reserve real tests only for the few behavior changes.

**Architecture:** Surgical source-only edits, batched per package so each publishable package ships **one** `patch` changeset for its low-band fixes. The template package is `changeset`-ignored (no changeset, Biome not ESLint); the migrator behavior fixes (#51) and the build-bookkeeping fix (#52) carry Vitest coverage, and the two DS behavior nits (#37 id collision, #38 empty state) are proven in Storybook — everything else is edit + build/lint check.

**Tech Stack:** TypeScript 5.9, React 19.2 (React Compiler via Babel on DS/next-ui/next; tsup on next-auth/migrator), Tailwind v4 + semantic tokens, Radix UI, next-themes, Vitest 4 (migrator + DS Storybook browser mode), Biome (template only), changesets (pre-release `beta`, always `patch`). Node ≥ 22, pnpm workspaces.

## Global Constraints

- **pnpm only**, `engines.node >= 22`. Internal deps use `workspace:*`.
- **Never edit `dist/`.** Source is always under `src/`. Never import package internals (`@igrp/*/dist/...`) — use documented subpath exports.
- **One `patch` changeset per user-visible change** to a publishable package (`pnpm changeset`). **Never `major`/`minor`** — repo is in pre-release `beta` mode; `patch` increments the beta counter only. (Batch a package's low fixes under one changeset.)
- **Build in dependency order:** `next-auth → next-types → design-system → next-ui → next`. After any public-API change to a framework package, run `pnpm build:framework` before consuming downstream.
- **Lint is package-specific:** framework packages = ESLint + Prettier; `templates/demo-v1` = Biome. Do not cross-apply.
- **No publish without explicit per-task authorization.** This plan stages code + changesets only; versioning/publishing is a separate, authorized step using each package's `release` script (never `changeset publish`).
- **`templates/demo-v1` and the storybook package are `changeset`-ignored** (`.changeset/config.json:10-13`) — template/tooling items ship without a changeset; they note `Changeset: none`.

## Verification per package (low band)

| Package | Verify | Test? |
| --- | --- | --- |
| `design-system` | `pnpm build:ds` | Storybook (Vitest browser) only for #37/#38; rest build-only |
| `framework-next` | `pnpm build:next` + manual reason-through | no new runner |
| `framework-next-auth` | `pnpm build:auth` | tiny Vitest only if a suite already exists; else build-only |
| `framework-next-ui` | `pnpm build:next-ui` + manual | no new runner |
| `templates/demo-v1` | `pnpm build:demo` (runs Biome) | none — **no changeset** |
| `template-migrator` | `pnpm --filter @igrp/template-migrator test` | Vitest for #51; #52(a) build-only |

DS Storybook test command: `pnpm --filter @igrp/igrp-framework-react-design-system-storybook test:vitest -t "<name>"` (Vitest browser mode, no running Storybook needed; asserts from `storybook/test`).

---

## Task A (spec #31, #32, #34, #35, #36): design-system trivial nits — typos, placeholder copy, `size-*`, `cn()`, `"use client"`, meta-color comment

**Files:**
- `packages/design-system/src/components/horizon/input/switch.tsx` (#31: `fielName`→`fieldName` throughout; `:102` `bg-backsground`→`bg-background`)
- `packages/design-system/src/components/horizon/input/checkbox.tsx:102` (#31: `bg-backsground`→`bg-background`)
- `packages/design-system/src/components/custom/status-banner.tsx:41,44,57` (#32: placeholder defaults + `h-3 w-3`→`size-3`)
- `packages/design-system/src/hooks/use-meta-color.ts:6-10` (#34: comment that hexes mirror `--background`)
- `packages/design-system/src/components/custom/*.tsx` (#35: add `"use client"` to the three files missing it)
- `packages/design-system/src/components/horizon/alert-dialog.tsx:99` (#36: `cn()` not string-concat hover)

These are all `@igrp/igrp-framework-react-design-system`, pure source, no behavior change → **one** changeset, build-only.

- [ ] **#31 — fix typos.** In `switch.tsx`, rename the variable `fielName`→`fieldName` at all sites (`:39` decl, plus `:48,49,53,62,69,79,89,99`) and change `:102` `"bg-backsground"`→`"bg-background"`. In `checkbox.tsx:102` change `"bg-backsground"`→`"bg-background"`. Before→after example (`switch.tsx:102`): `"bg-backsground",` → `"bg-background",`.
- [ ] **#32 — `IGRPStatusBanner` defaults + size nit.** In `status-banner.tsx`: drop the placeholder defaults so forgotten props render nothing, not demo copy — `:41` `text = "Insert our Text here"` → `text = ""`, `:44` `badgeText = "Badge Text"` → `badgeText = ""`; and `:57` `iconClassName="h-3 w-3 fill-current"` → `iconClassName="size-3 fill-current"`.
- [ ] **#34 — `use-meta-color` comment.** Above `IGRP_META_THEME_COLORS` (`:6-10`) add a one-line comment: these mirror `--background` (light/dark) and must change together; can't use a CSS var in `<meta theme-color>`. Optionally correct the dark value to match the current `--background` if it has drifted (the spec notes `#09090b` is zinc-950 — confirm against `tokens.css` `.dark --background`; if it matches, leave the value, keep only the comment). No value change unless the token disagrees.
- [ ] **#35 — `"use client"` on Custom layer.** Add `"use client"` as line 1 to the three `custom/*.tsx` files that lack it (only `stats-card-mini.tsx` has it). Find them: `Grep -L "use client" packages/design-system/src/components/custom/*.tsx` (or read each). Add the directive verbatim above the first import.
- [ ] **#36 — `IGRPAlertDialog` hover via `cn()`.** `alert-dialog.tsx:99`: `const actionClassName = actionProps ? actionProps.className : ` `` `${strongColors.bg} hover:${strongColors.bg}` `` → `cn(strongColors.bg)` (the `bg` token already includes its hover variant; the manual `hover:${...}` concat duplicates/conflicts). Leave the `<Slot id={ref}>` wrap (`:112`) as-is — out of scope for this nit. Confirm `cn` is already imported (it is, used throughout the file).
- [ ] **Build.** `pnpm build:ds` — expect green (SWC→Babel→types). Grep gate for #31: `Grep "backsground|fielName" packages/design-system/src` returns **no** source hits (snapshot `.snap` hits are fine and will refresh in Task B).
- [ ] **Changeset (batched).** `pnpm changeset` → `@igrp/igrp-framework-react-design-system` → **patch**. Rename to `.changeset/r2-p3-ds-nits.md`, body:
  ```md
  ---
  "@igrp/igrp-framework-react-design-system": patch
  ---

  P3 polish: fix `bg-backsground` typo (Switch, Checkbox) and `fielName`→`fieldName`
  (Switch); `IGRPStatusBanner` no longer defaults to placeholder copy and uses
  `size-3`; document `use-meta-color` hexes mirror `--background`; add `"use client"`
  across the Custom layer; build `IGRPAlertDialog` action hover via `cn()`.
  ```
- [ ] **Commit.** `fix(design-system): P3 low-band nits (typos, copy, tokens, use-client)` — executor appends `Co-Authored-By`. (Tasks A and B both touch the DS; commit A first, or batch both DS commits — your call, but keep the single shared changeset.)

---

## Task B (spec #37, #38): design-system behavior nits — pagination id collision + chart empty-state

Two real behavior changes → Storybook proof. Same package as Task A; **fold into the Task A `r2-p3-ds-nits.md` changeset** (do not create a second DS changeset). Append a bullet to that changeset body covering #37/#38.

**Files:**
- `packages/design-system/src/components/horizon/data-table/pagination.tsx:151` (`useId()` already computed), `:233` (static `id="results-per-page"`)
- `packages/design-system/src/components/horizon/charts/{area,line-chart-inner,radar-chart-inner,radial/radial-chart-inner,bars/horizontal-chart-inner,bars/vertical-chart-inner}.tsx` (empty-state guard); `charts/pie.tsx:14-27` is the existing-fallback reference
- Storybook: `packages/design-system-storybook/src/stories/` (pagination story + one chart story)

- [ ] **#37 — use the existing `useId()`.** `pagination.tsx`: the numeric-pagination component already calls `const id = useId()` at `:151`. At `:233` the page-size `Select` hardcodes `id="results-per-page"` (collides when two tables render on one page) — and likely a matching `htmlFor` on its label. Replace both the `id` and any `htmlFor="results-per-page"` with the `id` value (e.g. `` id={`${id}-results-per-page`} `` and matching `htmlFor`). Read `:225-240` to catch the label pairing before editing.
- [ ] **#37 test (Storybook).** Add a story rendering **two** `IGRPDataTable`s with numeric pagination on one canvas. `play`: query all elements with id containing `results-per-page` (or the page-size selects by role) and assert the two ids differ. Run `pnpm --filter @igrp/igrp-framework-react-design-system-storybook test:vitest -t "<story>"` — expect green after the fix (and the assertion is meaningful: it would fail on the old static id).
- [ ] **#38 — shared chart empty-state.** Area/line/bar/radar/radial render an empty plot for `data: []` (only `pie.tsx` has a fallback). Add a shared guard rendered before the chart body when `data.length === 0`: a centered `"Sem dados"` (pt-PT default, **overridable** via an optional prop e.g. `emptyText?: string` — keep the pt-PT default per repo convention). Prefer one small shared helper/component imported by the five inners over five copies; place it next to `charts/lib.ts`. Each inner: `if (!data || data.length === 0) return <ChartEmptyState text={emptyText} />` before the recharts container.
- [ ] **#38 test (Storybook).** Add (or extend) one chart story with `data={[]}` and assert the empty-state text renders (and a non-empty story still renders the plot). `test:vitest -t "<story>"` green.
- [ ] **Build + snapshots.** `pnpm build:ds` green. Then refresh HTML snapshots invalidated by Task A's typo fix and these stories: `pnpm storybook` (terminal A) + `pnpm test:storybook` (terminal B); accept legitimate diffs with `pnpm --filter @igrp/igrp-framework-react-design-system-storybook test-storybook:update-snapshots` and commit `__snapshots__/`. The `bg-backsground`→`bg-background` change **will** legitimately alter Switch/Checkbox snapshots (`form.stories.tsx.snap`) — that diff is expected, not a regression.
- [ ] **Changeset.** Append to `r2-p3-ds-nits.md` body: a line covering `IGRPDataTable` numeric-pagination id collision and chart empty-state (`"Sem dados"`, overridable).
- [ ] **Commit.** `fix(design-system): unique pagination select id + chart empty-state` — executor appends `Co-Authored-By`.

---

## Task C (spec #39, #40, #41, #42): framework-next — error-code typing, dead `igrpSafeAwait`, regex parse, global-loading

All `@igrp/framework-next`. `#40` is a confirmed delete (grep below); the rest are surgical. Build + manual reason-through; **no fabricated test runner** (no DS-style Storybook here). **One** changeset.

**Files:**
- `packages/framework/next/src/errors.ts:53,59` (`code: string` → `IgrpErrorCode`) and `:31-38` (union)
- `packages/framework/next/src/lib/safe-await.ts` (delete — dead)
- `packages/framework/next/src/lib/sync-routes.ts:28-39` (regex → structured parse)
- `packages/framework/next/src/components/global-loading.tsx` (magic `'IGRP'` + English copy)

- [ ] **#40 — delete dead `igrpSafeAwait` (grep-confirmed no importers).** Confirm again at execution: `Grep "igrpSafeAwait|safe-await" packages templates apps` — the only non-spec hits are the file itself. If still zero importers/exports, **delete** `packages/framework/next/src/lib/safe-await.ts`. (Already verified during planning: only `safe-await.ts` and the spec reference it; it is not re-exported from any entry point.) If a surprise importer appears, instead keep it and add `unstable_rethrow(error)` before the tuple `catch` return — but the expected action is delete.
- [ ] **#39 — tighten `IgrpErrorCode` discrimination.** In `errors.ts`, type the base `code` as `IgrpErrorCode` instead of `string`: `:53` (`IgrpErrorShape.code`) and `:59` (`IgrpError.code`). The constructor (`:62`) currently takes `code: string` — narrow to `code: IgrpErrorCode` (or, to preserve the "consumers extend with custom codes" comment at `:28-30`, make the class generic: `IgrpError<C extends string = IgrpErrorCode>` with `readonly code: C`). Pick the **minimal** change that keeps every framework throw site compiling — read the throw sites and subclass constructors first (`Grep "extends IgrpError|new IgrpError|IgrpConfigError|IgrpAuthConfigError|IgrpLayoutDataError" packages/framework/next/src`). Reconcile the union with thrown codes: `IGRP_LAYOUT_DATA_FAILED` is declared but never thrown, `IGRP_APP_CODE_MISSING` is thrown (in `next-ui` sidebar-data-provider) — leave both in the union (it's the shared vocabulary), just ensure the base type is the union.
- [ ] **#41 — replace hand-rolled regex in `sync-routes`.** `sync-routes.ts:28-39` uses `/"([^"]+)"\s*:\s*(\{[\s\S]*?});?/g` to find empty `{}` param objects; this breaks on nested objects/comments/trailing commas and runs inside `after()` (silent mis-sync). Replace with a structured parse: `JSON.parse(paramMapBody)` then iterate entries and test `Object.keys(value).length === 0`. Wrap the parse in try/catch — on parse failure, `console.warn` and return (same fail-safe posture as the existing `:23-26` guard) rather than throwing inside `after()`. Read the call site (the function's caller — `Grep "syncRoutes\|sync-routes" packages/framework/next/src`) and `:40-60` to confirm `paramMapBody` is a JSON-shaped string; if it is a JS object literal (not strict JSON), prefer having the template emit a JSON sidecar (note it in **Couldn't fully plan** if the source shape blocks a clean `JSON.parse`).
- [ ] **#42 — `IGRPGlobalLoading` magic string + i18n.** `global-loading.tsx`: extract `const IGRP_SYSTEM_APP_CODE = 'IGRP'` (module scope) and use it at `:4`. Make the loading copy pt-PT and overridable: add an optional prop (e.g. `loadingText?: string`) defaulting to a pt-PT string — replace `Loading...`/`Loading {name}...` (`:5`,`:10`) with the pt-PT default (e.g. `` `A carregar${app?.name ? ` ${app.name}` : ''}…` ``). Keep the component server-async signature.
- [ ] **Build + manual.** `pnpm build:next` — green. Manual reason-through: (a) `#39` — every throw site / subclass still type-checks; (b) `#40` — no broken import after delete; (c) `#41` — feed a nested-object param map mentally and confirm the new parse classifies empty vs non-empty correctly; (d) `#42` — preview-mode and normal both render (no behavior branch touched). No new test file.
- [ ] **Changeset (batched).** `pnpm changeset` → `@igrp/framework-next` → **patch**. Rename `.changeset/r2-p3-next-nits.md`:
  ```md
  ---
  "@igrp/framework-next": patch
  ---

  P3 polish: type `IgrpError.code` as `IgrpErrorCode` (real discrimination);
  remove dead `igrpSafeAwait` (would have swallowed `NEXT_REDIRECT`); parse
  `sync-routes` param map via `JSON.parse` instead of a brittle regex; extract
  `IGRP_SYSTEM_APP_CODE` and make `IGRPGlobalLoading` copy a pt-PT overridable prop.
  ```
- [ ] **Commit.** `refactor(next): P3 low-band fixes (error codes, dead code, route parse, loading copy)` — executor appends `Co-Authored-By`.

---

## Task D (spec #43): framework-next-auth — `as any`→`as object` + claims invariant comment

`@igrp/framework-next-auth`. The `as any` cleanup is a one-line behavior-neutral fix; the claims item is a comment/guard; the barrel item is confirm-only (no change).

**Files:**
- `packages/framework/next-auth/src/session.ts:19-21` (`hasAccessToken` uses `as any`)
- `packages/framework/next-auth/src/claims.ts` (add invariant comment)

- [ ] **#43a — `as any`→`as object`.** `session.ts:20`: `return !!(s && typeof s === 'object' && 'accessToken' in (s as any));` → `'accessToken' in (s as object)`. (After the `typeof s === 'object'` guard, `s` is safely an object; `in` needs an object operand, not `any`.)
- [ ] **#43b — claims invariant comment.** In `claims.ts`, add a comment at the top of the claims-allow logic stating the invariant: `claims.ts` performs **no JWS verification**, so `claimsAllow` (and any claim read) must **never** gate a server-side security decision on an unverified token — it is advisory/UI-shaping only. If there is a natural single guard point, add a short comment there; do not add runtime verification (out of scope). Read `claims.ts` first to place the comment where `claimsAllow`/claim consumption happens.
- [ ] **#43c — barrel (confirm-only, no change).** `index.ts` is intentionally a partial barrel (subpath exports are the contract). Confirm by reading `index.ts` + `package.json` `exports`; **make no change** — note "confirmed intentional" in the commit body.
- [ ] **Test (tiny Vitest, only if a suite exists).** `Glob packages/framework/next-auth/**/*.test.ts` — if next-auth already has a Vitest suite, add a 3-case test for `hasAccessToken` (object with `accessToken` → true; object without → false; non-object/null → false) and run it. If **no** suite exists, do **not** scaffold a runner for a type-only cast change — rely on `pnpm build:auth`. (Expected: no suite in next-auth → build-only.)
- [ ] **Build.** `pnpm build:auth` — green (tsup). Since next-auth is the chain root, run `pnpm build:framework` if anything downstream type-depends (the `as object` change is non-breaking; build:auth suffices for verification).
- [ ] **Changeset.** `pnpm changeset` → `@igrp/framework-next-auth` → **patch**. `.changeset/r2-p3-auth-nits.md`:
  ```md
  ---
  "@igrp/framework-next-auth": patch
  ---

  P3 polish: `hasAccessToken` narrows via `as object` instead of `as any`; document
  the claims invariant that `claimsAllow` never gates a server-side decision on an
  unverified token.
  ```
- [ ] **Commit.** `refactor(next-auth): tighten hasAccessToken cast + document claims invariant` — executor appends `Co-Authored-By`.

---

## Task E (spec #44, #45, #46, #47): framework-next-ui — logout url prop, notification date i18n, freeze-claims (confirm), keyboard-select (confirm)

`@igrp/framework-next-ui`. #44/#45 are clear fixes; #46/#47 are PLAUSIBLE → confirm-then-fix-or-document. Build + manual; **one** changeset.

**Files:**
- `packages/framework/next-ui/src/components/templates/nav-user.tsx:152` (`<Link href="/logout">`)
- `packages/framework/next-ui/src/components/templates/notifications.tsx:73` (`timestamp.toLocaleString()`)
- `packages/framework/next-ui/src/permissions/section-permissions.tsx:25` (`useState(initialState)`)
- `packages/framework/next-ui/src/components/templates/menus/sub-leaf-link.tsx:45` (`onSelect={(e) => e.preventDefault()}`)

- [ ] **#44 — `logoutUrl` prop.** `nav-user.tsx`: profile/notifications/settings are already basePath-relative props; logout is hardcoded `<Link href="/logout">` (`:152`), breaking under basePath. Add `logoutUrl?: string` to the component props defaulting to `'/logout'`, and use it: `<Link href={logoutUrl}>`. Match the existing sibling prop naming/threading (read the props interface + how `profileUrl` etc. flow in).
- [ ] **#45 — notification date formatting.** `notifications.tsx:73` renders `notification.timestamp.toLocaleString()` (browser-locale, unstable; RSC `Date`-serialization hazard). Fix: (a) type `timestamp` as ISO `string | number` in the notification type (find it via `Grep "timestamp" packages/framework/next-ui/src` / the `IGRPNotificationArgs` type — note it may live in `next-types`; if so, **defer the type change** and only format defensively here, flagging the cross-package type in **Couldn't fully plan**); (b) format with a memoized `Intl.DateTimeFormat` (pt-PT default, overridable via prop), constructing `new Date(timestamp)` client-side. Minimum viable in-package: wrap in `new Date(...)` + a module-scope `Intl.DateTimeFormat('pt-PT', {...})` and call `.format(new Date(notification.timestamp))`.
- [ ] **#46 — confirm freeze-claims intent (PLAUSIBLE).** `section-permissions.tsx:25` `useState(initialState)` ignores later `initialState` prop changes. Read the file's JSDoc + how the component is used. The spec says JSDoc suggests Phase-2 updates come via `setState` (mount-once is **intentional**). **Decision:** if JSDoc confirms mount-once intent → **document** (no behavior change): rename the prop to `initialState` if not already, and add/confirm a JSDoc line "seeded once at mount; later updates via the returned setter". If JSDoc says it should track the prop → add `useEffect(() => setState(initialState), [initialState])`. Default expectation per spec: **document as intentional**, no `useEffect`.
- [ ] **#47 — confirm keyboard activation (PLAUSIBLE, manual).** `sub-leaf-link.tsx:45` `onSelect={(e) => e.preventDefault()}` on a `DropdownMenuItem asChild` wrapping a `Link` (collapsed-sidebar folder dropdown). `preventDefault` on `onSelect` may block Enter-to-activate/close. **Manual keyboard test** in dev (`pnpm dev:demo`, collapse sidebar, open a folder dropdown, Tab to a sub-item, press Enter): if Enter navigates + closes correctly, the `preventDefault` is harmless (Radix still closes) → **leave it, add a one-line comment** explaining why it stays. If Enter does **not** navigate, **drop** `preventDefault` (the `Link` handles navigation) or close the menu explicitly in the handler. Record the observed behavior in the commit body.
- [ ] **Build + manual.** `pnpm build:next-ui` — green. Manual: #44 logout link respects a basePath prop; #45 a notification renders a stable pt-PT date; #46/#47 per their decisions above. No new test runner.
- [ ] **Changeset (batched).** `pnpm changeset` → `@igrp/framework-next-ui` → **patch**. `.changeset/r2-p3-next-ui-nits.md`:
  ```md
  ---
  "@igrp/framework-next-ui": patch
  ---

  P3 polish: `IGRPTemplateNavUser` accepts a `logoutUrl` prop (basePath-safe);
  notifications format timestamps with a stable pt-PT `Intl.DateTimeFormat`;
  clarify `IGRPSectionPermissions` seeds claims once at mount; verify keyboard
  activation on collapsed-sidebar sub-items.
  ```
  (Adjust the changeset bullets to match the #46/#47 outcomes actually applied.)
- [ ] **Commit.** `fix(next-ui): P3 low-band fixes (logout url, notification i18n, perms/keyboard nits)` — executor appends `Co-Authored-By`.

---

## Task F (spec #48, #49, #50): templates/demo-v1 — pt-PT copy, themes.css comment, module-arch note. NO CHANGESET.

`templates/demo-v1` is `changeset`-ignored and uses **Biome**, not ESLint/Prettier. Copy + doc only.

**Files:**
- `templates/demo-v1/src/config/login.ts:6,17-35` (#48: English copy)
- `templates/demo-v1/src/styles/themes.css:1-3` (#49: `bg-transparent` comment)
- `templates/demo-v1/CLAUDE.md` or `README` (#50: module-arch divergence note)

- [ ] **#48 — translate login copy to pt-PT.** `config/login.ts`: `:6` `loginButton: "Login Now"` and `:17-35` `carouselItems` titles/descriptions are English amid a pt-PT template. Translate to pt-PT (copy-only; the strings are already externalized in this config). Read the full `carouselItems` array first to translate each title/description in place.
- [ ] **#49 — document `themes.css` transparent body.** `styles/themes.css:1-3` declares `body { @apply … bg-transparent }` (unlayered, after `globals.css`'s `@layer base body { bg-background }`); the visible bg comes from `IGRPRootLayout`'s `<body className="bg-background">`. Add a one-line comment above the rule: `bg-transparent` is deliberate — the framework's root layout owns the body background; this avoids double-painting. No value change.
- [ ] **#50 — module-arch divergence note.** Add a short note to `templates/demo-v1/CLAUDE.md` (preferred) stating: the `(igrp)/(demo)/<route>/page.tsx` logic-in-page pattern + `src/actions/igrp/` is a **framework-consumption demo**; real features should follow `igrp-module-architecture` (`(generated)`→`_features`→`src/actions/simple`). Doc-only.
- [ ] **Verify (no changeset).** `pnpm build:demo` — green (this runs Biome format/lint on the template). Confirm no Biome errors on `login.ts`. Do **not** run `pnpm changeset` for this task (template is changeset-ignored).
- [ ] **Commit.** `chore(demo-v1): pt-PT login copy + document themes.css body bg and module-arch divergence` — executor appends `Co-Authored-By`. (Per the template-migration rule, template changes normally ship as a migrator migration — but #48/#49/#50 are copy/doc-only; if the drift gate flags them at release, fold them into a migration then. Flag in **Couldn't fully plan** if the drift gate must pass before merge.)

---

## Task G (spec #51): template-migrator — decline/abort, manifest self-heal persistence, deps.bump dual-bucket undo. VITEST.

`@igrp/template-migrator`. These are behavior changes → Vitest. **One** changeset.

**Files:**
- `packages/template-migrator/src/commands/apply.ts:26` (self-heal `lock.template` not persisted on no-op), `:46-49` (interactive decline → `continue`)
- `packages/template-migrator/src/apply.ts:106-121` (`deps.bump` undo: dep in both `dependencies` and `devDependencies`)
- `packages/template-migrator/src/__tests__/` (tests)

- [ ] **#51a — decline handling.** `commands/apply.ts:46-49`: a non-`--yes` decline currently `console.log("  Skipped.")` + `continue`, leaving later migrations to apply on a missing base. Until #11's toposort lands, treat a decline as **abort the run** (or at minimum a loud warning + abort). Change `continue` to a clear `console.warn` + `return` (abort), since the file order is a strict linear chain and skipping one breaks successors. Read `:37-49` to place the abort correctly inside the loop.
- [ ] **#51b — persist `manifest.template` self-heal on no-op paths.** `commands/apply.ts:26` assigns `lock.template = manifest.template` in memory, but the early returns (`:31` not-found, `:34` nothing-to-apply) exit **before** the lock is written, so the self-heal is lost on no-op runs. Fix: write the lock on those no-op paths too (call the existing `writeLock(appRoot, lock)` before each early `return` where `lock.template` changed), **or** drop the in-memory assignment if it's only meaningful alongside a real apply. Prefer persisting (the comment says it's for convergence). Read the `writeLock` signature + where it's called on the success path to mirror it.
- [ ] **#51c — `deps.bump` dual-bucket undo.** `apply.ts:106-121`: the apply loop checks `dependencies` **then** `devDependencies` (else-if), and undo records one `prevRange` per dep — unfaithful if a dep exists in **both** buckets (records/restores only one). Fix: record both buckets when present (e.g. `prevRanges[dep]` keyed to capture both, or bump+restore each bucket independently), or detect the both-buckets case and error clearly. Simplest faithful fix: iterate both `dependencies` and `devDependencies` independently (not else-if), and have the undo payload carry per-bucket previous ranges. Adjust the `deps.bump` step/undo type if it only models one bucket today (read the `MigrationStep` `deps.bump` type first).
- [ ] **Tests (Vitest).** Add to `src/__tests__/`: (a) declining a migration aborts the run (no later migration applied); (b) a no-op `apply` (nothing pending) still persists `lock.template` when it differed; (c) `deps.bump` undo restores a dep present in **both** `dependencies` and `devDependencies`. Run `pnpm --filter @igrp/template-migrator test` — write the test first (red), implement, green.
- [ ] **Changeset (batched with Task H).** Create **one** migrator changeset covering #51 + #52 (see Task H) — do not create two. Defer the changeset write to Task H's step.
- [ ] **Commit.** `fix(template-migrator): abort on decline, persist self-heal, faithful deps.bump undo` — executor appends `Co-Authored-By`.

---

## Task H (spec #52): template-migrator — tsconfig.build exclude `*.test.ts`; version-line decision

`@igrp/template-migrator`. #52(a) is a one-line config fix (build-only); #52(b) is a confirm/record decision.

**Files:**
- `packages/template-migrator/tsconfig.build.json:6` (exclude pattern)

- [ ] **#52a — align the tsc exclude.** `tsconfig.build.json:6` excludes `src/**/__tests__/**` but not `src/**/*.test.ts` (which `vitest.config.ts` picks up), so a stray `*.test.ts` outside `__tests__/` could be type-emitted to `dist`. Change the exclude to also cover test files: `"exclude": ["node_modules", "dist", "src/**/__tests__/**", "src/**/*.test.ts", "src/**/*.test.tsx"]`. Verify `pnpm --filter @igrp/template-migrator build` emits no test files to `dist` (check `dist/` has no `*.test.*`).
- [ ] **#52b — version-line decision (confirm/record, no code).** The migrator's published version line vs the framework `0.1.0-beta.*` line: query the registry `pnpm view @igrp/template-migrator version --registry=https://sonatype.nosi.cv/repository/igrp/` and record the intended baseline decision in the commit body (going forward, `patch`-only changesets keep it on its line). No code change for (b); if the baseline is genuinely undecided, note it in **Couldn't fully plan** rather than guessing.
- [ ] **Build.** `pnpm --filter @igrp/template-migrator build` — green; `dist/` free of test files. Re-run `pnpm --filter @igrp/template-migrator test` to confirm Task G + H together are green.
- [ ] **Changeset (single migrator changeset for #51 + #52).** `pnpm changeset` → `@igrp/template-migrator` → **patch**. `.changeset/r2-p3-migrator-nits.md`:
  ```md
  ---
  "@igrp/template-migrator": patch
  ---

  P3 polish: declining an interactive migration aborts the run (no later migration
  on a missing base); `manifest.template` self-heal persists on no-op `apply`;
  `deps.bump` undo is faithful when a dep is in both `dependencies` and
  `devDependencies`; `tsconfig.build` excludes `*.test.ts` so stray tests aren't
  emitted to `dist`.
  ```
- [ ] **Commit.** `chore(template-migrator): exclude test files from build emit` — executor appends `Co-Authored-By`.

---

## Final verification (before handing off P3)

- [ ] `pnpm build:ds` green; `Grep "backsground|fielName" packages/design-system/src` = no source hits; #37/#38 stories green under `test:vitest`; DS snapshots refreshed (Switch/Checkbox diff expected).
- [ ] `pnpm build:next` green; `safe-await.ts` deleted with no broken import; #39 throw sites compile.
- [ ] `pnpm build:auth` green (and `pnpm build:framework` if verifying downstream).
- [ ] `pnpm build:next-ui` green; #46/#47 decisions recorded in commit bodies.
- [ ] `pnpm build:demo` green (Biome clean); **no template changeset**.
- [ ] `pnpm --filter @igrp/template-migrator test` green (new #51 cases); `dist/` free of `*.test.*`.
- [ ] Exactly **five** `patch` changesets staged: DS, next, next-auth, next-ui, migrator. Template = none.
- [ ] No `dist/` files staged; no version bumps; nothing published.

## Couldn't fully plan

#33 (DS third-party type re-exports, `index.ts:877-892`) was descoped from the tasks above — it needs a grep of template/app consumers (`Grep "from '@igrp/igrp-framework-react-design-system'" ... | ColumnDef|DateRange|SortingState`) to decide keep-and-document vs drop; the spec marks it "needs confirmation," so it is a confirm/decide item to run before touching `index.ts` (low risk either way — fold into the Task A DS changeset if kept-and-documented). #41's `JSON.parse` and #45's `IGRPNotificationArgs.timestamp` type may be blocked if the source shape isn't strict JSON / the type lives in `next-types` (cross-package) — confirm at execution and defer the cross-package type change if so. #52(b) version baseline and the #48-50 template/drift-gate interaction may need a maintainer call.
