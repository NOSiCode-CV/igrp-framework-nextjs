---
id: 13-resync-beta145
date: 2026-06-04
targetFrameworkVersion: 0.1.0-beta.145
requires: ["12-template-resync"]
steps:
  - type: file.write
    path: src/lib/dal.ts
    mode: replace
    from: payload/13/lib/dal.ts
  - type: deps.bump
    manifest: package.json
    ranges:
      "@igrp/framework-next": "0.1.0-beta.145"
      "@igrp/framework-next-ui": "0.1.0-beta.144"
      "@igrp/igrp-framework-react-design-system": "0.1.0-beta.135"
---

# Migration Guide — Resync to framework `0.1.0-beta.145` (`13-resync-beta145`)

A follow-up **resync migration** to `12-template-resync`. It captures two changes the
template picked up alongside the `framework@0.1.0-beta.145` release that migration `12`
did not cover:

- **`src/lib/dal.ts` re-captured** — the import block was reordered (Biome import sort)
  after migration `12` snapshotted the file, so the payload and the live template
  diverged. This re-capture realigns them; the runtime behaviour is unchanged.
- **Dependency pins advanced** to the versions paired with this template snapshot:
  `@igrp/framework-next@0.1.0-beta.145`, `@igrp/framework-next-ui@0.1.0-beta.144`, and
  `@igrp/igrp-framework-react-design-system@0.1.0-beta.135`. `@igrp/framework-next-auth`
  and `@igrp/framework-next-types` are unchanged from migration `12` (still
  `0.1.0-beta.136` / `0.1.0-beta.138`) and are not re-pinned here.

## What changed

### Files re-captured (`file.write`, full replace)

| File | Area |
|---|---|
| `src/lib/dal.ts` | data-access layer — import order only |

### Dependency bumps (`deps.bump`)

| Dependency | Version |
|---|---|
| `@igrp/framework-next` | `0.1.0-beta.145` |
| `@igrp/framework-next-ui` | `0.1.0-beta.144` |
| `@igrp/igrp-framework-react-design-system` | `0.1.0-beta.135` |

## Notes

- The `dal.ts` replacement is import-ordering only — no logic change. Review the diff in
  your app before committing if you have local customizations to this file.
- Run `igrp-migrate plan` first to preview, then `igrp-migrate apply`.
