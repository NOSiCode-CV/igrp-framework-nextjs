---
description: IGRP design system usage — three-layer picker (Horizon / Primitives / Custom), source-verified prop shapes for IGRPForm, IGRPDataTable, IGRP*Chart, the repo-wide UI hard rules, and the permission-gating rules for pages, server actions, and components. Auto-applies when editing React/TSX code inside this IGRP template.
globs:
  - "src/**/*.{ts,tsx}"
alwaysApply: false
---

# IGRP design system rules

This project uses `@igrp/igrp-framework-react-design-system`. All UI must follow the conventions documented in the canonical skill.

**Read `.agents/skills/igrp-design-system/SKILL.md` and follow its instructions** before writing any UI code. It documents:

- The three-layer picker: Horizon (`IGRP*`) → Custom → Primitives (escape hatch only)
- Hard rules: `'use client'`, `IGRPForm` + Zod for every form, semantic tokens only, `size-*` not `w-* h-*`, `flex gap-*` not `space-*`
- Common pitfalls (raw `<Input>` inside `IGRPForm`, raw palette colors, missing `'use client'`, etc.)

Deep references live alongside SKILL.md under `.agents/skills/igrp-design-system/references/` — load them on demand for the family you're working with:

- `forms.md` — `IGRPForm` + Zod + all `IGRPInput*` (load when any form is in scope)
- `data-table.md` — `IGRPDataTable`, `createIGRPColumnHelper`, the `actions` prop with typed row actions
- `charts.md` — `IGRPAreaChart` / `IGRPVerticalBarChart` / `IGRPLineChart` / … with `categoryKey` + `bars: IGRPBarConfig[]` + `valueFormatter: (n) => string`
- `horizon.md` — shared `IGRPInputProps`, naming conventions
- `primitives.md` — when to drop down, CVA variants
- `utilities.md` — `cn()`, `IGRPColors`, hooks
- `custom.md` — domain pieces

@.agents/skills/igrp-design-system/SKILL.md

## Permissions — there is NO default-deny

A page with no permission check is fully open and deep-linkable; a hidden menu item is navigation UX, not enforcement.

- Page needing a permission → `await igrpAssertAuthorize("<perm>")` from `@igrp/framework-next` on the **first line** of the server component (denied → 403).
- Page not needing one → record that in a one-line comment, so it reads as a decision rather than an omission.
- Server action → `igrpAuthorize(name)` (boolean) + `{ ok: false, code: "forbidden" }`; an action has no 403 boundary. Always gate the action behind a mutating control — the control itself is cosmetic.
- Client UI → `<IGRPAuthorization permission="…">` or `usePermissions().isAllowed(…)` from `@igrp/framework-next-ui`. Never add a `permission` prop to a design-system component.

Full guide: `docs/PERMISSIONS.md`. Same rule in `AGENTS.md`.
